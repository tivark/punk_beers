import { createElWithClass, setButtonEffect } from "../helpers/helpers.js";

class AutorizathionForm{
  constructor(){

  }

  createHtml(){
    const formLayer = createElWithClass('div', 'feedback-form-layer');
    const formWrapper = createElWithClass('div', 'form-wrapper');
  }
}